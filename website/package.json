{
  "name": "mcp-for-unity-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "prebuild": "npm run validate:failures && npm run validate:evidence && npm run validate:environment && npm run validate:ja-display && npm run validate:web-failures && npm run validate:signatures && npm run validate:taxonomy && npm run validate:unity-failures && npm run validate:coverage",
    "build": "docusaurus build",
    "validate:failures": "node scripts/validate-failures.mjs",
    "validate:evidence": "node scripts/validate-failure-evidence.mjs",
    "validate:environment": "node scripts/validate-failure-environment.mjs",
    "validate:ja-display": "node scripts/validate-failure-ja-display.mjs",
    "validate:web-failures": "node scripts/collect-web-failures.mjs",
    "validate:signatures": "node scripts/normalize-failure-signatures.mjs",
    "validate:taxonomy": "node scripts/classify-failures.mjs",
    "validate:unity-failures": "node scripts/collect-unity-official-failures.mjs",
    "validate:coverage": "node scripts/failure-coverage.mjs",
    "collect:web-failures": "node scripts/collect-web-failures.mjs --live",
    "collect:unity-failures": "node scripts/collect-unity-official-failures.mjs --live",
    "check:failure-sources": "node scripts/check-failure-sources.mjs",
    "coverage:failures": "node scripts/failure-coverage.mjs",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "gen:brand": "node scripts/gen-brand-assets.mjs"
  },
  "dependencies": {
    "@docusaurus/core": "^3.6.3",
    "@docusaurus/plugin-client-redirects": "^3.6.3",
    "@docusaurus/preset-classic": "^3.6.3",
    "@easyops-cn/docusaurus-search-local": "^0.46.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "^3.6.3",
    "@docusaurus/types": "^3.6.3",
    "docusaurus-plugin-goatcounter": "^4.0.0",
    "sharp": "^0.35.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
