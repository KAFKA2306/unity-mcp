name: Design conformance

on:
  pull_request:
    branches: [beta]
  push:
    branches: [beta]

permissions:
  contents: read

jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify canonical design contract
        run: node website/src/css/.kafka-design/portable-conformance.mjs --consumer "$GITHUB_WORKSPACE"
